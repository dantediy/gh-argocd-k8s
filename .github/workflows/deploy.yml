name: Deployment
on:
  workflow_dispatch:
  push:
    branches:
      - release/dev
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      # - name: Get code
      #   uses: actions/checkout@v3

      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Extract metadata (tags, labels) for Docker
      #   id: meta
      #   uses: docker/metadata-action@v4
      #   with:
      #     images: dantediy/gh-argocd-k8s
      #     tags: |
      #       type=semver,pattern={{version}}
      #       type=semver,pattern={{major}}.{{minor}}
      #       type=raw,value={{sha}},enable=${{ github.ref_type != 'tag' }}
      # - name: Build and push docker images
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: .
      #     file: ./Dockerfile
      #     push: true
      #     tags: |
      #       ${{ steps.meta.outputs.tags }}
      #       ${{ github.ref_name }}
      #       ${{ steps.meta.outputs.tags }}-${{ github.ref_name }}
      #     labels: ${{ steps.meta.outputs.labels }}
      - name: Extract branch name
        shell: bash
        run: echo "branch=$(echo ${GITHUB_REF#refs/heads/release/})" >> $GITHUB_OUTPUT
        id: extract_branch
      - name: Echo
        run: |
         echo "Tag name from GITHUB_REF_NAME: ${{steps.extract_branch.outputs.branch}}"